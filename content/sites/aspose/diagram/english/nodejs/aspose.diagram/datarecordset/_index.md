---
title: "DataRecordSet"
linktitle: "DataRecordSet"
second_title: "Aspose.Diagram for Node.js via Java API Reference"
description: "Stores, formats, refreshes, and exposes data queried from a database in Microsoft Visio."
type: docs
weight: 600
url: /nodejs/aspose.diagram/datarecordset/
---

## DataRecordSet class

Stores, formats, refreshes, and exposes data queried from a database in Microsoft Visio.

```js
new DataRecordSet()
```

Constructor.

## Methods

| Name | Description |
| --- | --- |
| [getADOData()](#getadodata) | Contains XML that conforms to the ADO classic XML schema for an ADO recordset and that describes the data in the data re |
| [getAutoLinkComparison()](#getautolinkcomparison) | Defines a rule that compares a column in the parent DataRecordset element with a shape data item from the last successfu |
| [getChecksum()](#getchecksum) | A checksum value, generated by Visio, and based on data-recordset properties. Set this attribute to 0; Visio recalculate |
| [getCommand()](#getcommand) | The command string used to query data from the data source. |
| [getConnectionID()](#getconnectionid) | The connection ID for the associated DataConnection object. Does not exist for XML data sources. |
| [getDataColumns()](#getdatacolumns) | Contains all the DataColumn elements in a data recordset. |
| [getID()](#getid) | The data recordset ID, unique within the document. |
| [getName()](#getname) | The display (or "friendly") name of the data recordset. |
| [getNextRowID()](#getnextrowid) | The next available Visio row ID. |
| [getOptions()](#getoptions) | Options to apply to the data recordset. Possible values can be any combination of one or more of those shown in the foll |
| [getPrimaryKeys()](#getprimarykeys) | Identifies one or more primary-key columns in the data recordset. |
| [getRefreshConflicts()](#getrefreshconflicts) | Indicates a row in the data recordset linked to a shape that is in conflict after the data recordset is refreshed. RowID |
| [getRefreshInterval()](#getrefreshinterval) | How often (in minutes) Visio refreshes the data recordset automatically. This value must be 1 or larger. |
| [getRefreshNoReconciliationUI()](#getrefreshnoreconciliationui) | Whether the data-reconciliation user interface should be disabled. True (1) to disable the user interface (UI). False (0 |
| [getRefreshOverwriteAll()](#getrefreshoverwriteall) | Whether to overwrite user changes to shape data items in shapes linked to data when the data recordset is refreshed. The |
| [getReplaceLinks()](#getreplacelinks) | Defines how shape-data links are treated when shapes are copied or cut. 1 to replace existing links in the target shape. |
| [getRowMaps()](#getrowmaps) | Maps a data-recordset row to a shape. RowID - Row ID of the row, unique within the data recordset. ShapeID - Shape ID of |
| [getRowOrder()](#getroworder) | Whether to use the order of the rows in the data recordset as the primary key. True (1) if row IDs are determined by row |
| [getTimeRefreshed()](#gettimerefreshed) | The date and time the data recordset was last refreshed. |
| [refresh(connectionType)](#refresh) | Executes the query string associated with the connected (non-XML-based) DataRecordset and updates linked shapes with new |
| [setADOData()](#setadodata) | Contains XML that conforms to the ADO classic XML schema for an ADO recordset and that describes the data in the data re |
| [setChecksum()](#setchecksum) | A checksum value, generated by Visio, and based on data-recordset properties. Set this attribute to 0; Visio recalculate |
| [setCommand()](#setcommand) | The command string used to query data from the data source. |
| [setConnectionID()](#setconnectionid) | The connection ID for the associated DataConnection object. Does not exist for XML data sources. |
| [setID()](#setid) | The data recordset ID, unique within the document. |
| [setName()](#setname) | The display (or "friendly") name of the data recordset. |
| [setNextRowID()](#setnextrowid) | The next available Visio row ID. |
| [setOptions()](#setoptions) | Options to apply to the data recordset. Possible values can be any combination of one or more of those shown in the foll |
| [setRefreshInterval()](#setrefreshinterval) | How often (in minutes) Visio refreshes the data recordset automatically. This value must be 1 or larger. |
| [setRefreshNoReconciliationUI()](#setrefreshnoreconciliationui) | Whether the data-reconciliation user interface should be disabled. True (1) to disable the user interface (UI). False (0 |
| [setRefreshOverwriteAll()](#setrefreshoverwriteall) | Whether to overwrite user changes to shape data items in shapes linked to data when the data recordset is refreshed. The |
| [setReplaceLinks()](#setreplacelinks) | Defines how shape-data links are treated when shapes are copied or cut. 1 to replace existing links in the target shape. |
| [setRowOrder()](#setroworder) | Whether to use the order of the rows in the data recordset as the primary key. True (1) if row IDs are determined by row |

### getADOData() {#getadodata}

Contains XML that conforms to the ADO classic XML schema for an ADO recordset and that describes the data in the data recordset.

### getAutoLinkComparison() {#getautolinkcomparison}

Defines a rule that compares a column in the parent DataRecordset element with a shape data item from the last successful automatic linking action performed in the user interface.

### getChecksum() {#getchecksum}

A checksum value, generated by Visio, and based on data-recordset properties. Set this attribute to 0; Visio recalculates this value at runtime.

### getCommand() {#getcommand}

The command string used to query data from the data source.

### getConnectionID() {#getconnectionid}

The connection ID for the associated DataConnection object. Does not exist for XML data sources.

### getDataColumns() {#getdatacolumns}

Contains all the DataColumn elements in a data recordset.

### getID() {#getid}

The data recordset ID, unique within the document.

### getName() {#getname}

The display (or "friendly") name of the data recordset.

### getNextRowID() {#getnextrowid}

The next available Visio row ID.

### getOptions() {#getoptions}

Options to apply to the data recordset. Possible values can be any combination of one or more of those shown in the following table. The value of the property is OptionsValue integer constant.

### getPrimaryKeys() {#getprimarykeys}

Identifies one or more primary-key columns in the data recordset.

### getRefreshConflicts() {#getrefreshconflicts}

Indicates a row in the data recordset linked to a shape that is in conflict after the data recordset is refreshed. RowID - Indicates a row in the data recordset linked to a shape that is in conflict after the data recordset is refreshed. ShapeID - Shape ID of the shape involved in the conflict. PageID - Page ID of the shape involved in the conflict.

### getRefreshInterval() {#getrefreshinterval}

How often (in minutes) Visio refreshes the data recordset automatically. This value must be 1 or larger.

### getRefreshNoReconciliationUI() {#getrefreshnoreconciliationui}

Whether the data-reconciliation user interface should be disabled. True (1) to disable the user interface (UI). False (0) to enable the UI. The value of the property is BOOL integer constant.

### getRefreshOverwriteAll() {#getrefreshoverwriteall}

Whether to overwrite user changes to shape data items in shapes linked to data when the data recordset is refreshed. The value of the property is BOOL integer constant.

### getReplaceLinks() {#getreplacelinks}

Defines how shape-data links are treated when shapes are copied or cut. 1 to replace existing links in the target shape. 0 to maintain existing links in the target shape. If this attribute is absent, Visio asks the user whether to replace links on copy or cut.

### getRowMaps() {#getrowmaps}

Maps a data-recordset row to a shape. RowID - Row ID of the row, unique within the data recordset. ShapeID - Shape ID of the shape linked to data in the data-recordset row identified by RowID. PageID - Page ID of the shape linked to data in the data-recordset row identified by RowID.

### getRowOrder() {#getroworder}

Whether to use the order of the rows in the data recordset as the primary key. True (1) if row IDs are determined by row order. False (0) if row IDs are determined by values in the primary key column(s) of the data recordset. The value of the property is BOOL integer constant.

### getTimeRefreshed() {#gettimerefreshed}

The date and time the data recordset was last refreshed.

### refresh(connectionType) {#refresh}

Executes the query string associated with the connected (non-XML-based) DataRecordset and updates linked shapes with new data from the data source returned by the query.

| Parameter | Type | Description |
| --- | --- | --- |
| connectionType | Number | DataConnectionType |

### setADOData() {#setadodata}

Contains XML that conforms to the ADO classic XML schema for an ADO recordset and that describes the data in the data recordset.

### setChecksum() {#setchecksum}

A checksum value, generated by Visio, and based on data-recordset properties. Set this attribute to 0; Visio recalculates this value at runtime.

### setCommand() {#setcommand}

The command string used to query data from the data source.

### setConnectionID() {#setconnectionid}

The connection ID for the associated DataConnection object. Does not exist for XML data sources.

### setID() {#setid}

The data recordset ID, unique within the document.

### setName() {#setname}

The display (or "friendly") name of the data recordset.

### setNextRowID() {#setnextrowid}

The next available Visio row ID.

### setOptions() {#setoptions}

Options to apply to the data recordset. Possible values can be any combination of one or more of those shown in the following table. The value of the property is OptionsValue integer constant.

### setRefreshInterval() {#setrefreshinterval}

How often (in minutes) Visio refreshes the data recordset automatically. This value must be 1 or larger.

### setRefreshNoReconciliationUI() {#setrefreshnoreconciliationui}

Whether the data-reconciliation user interface should be disabled. True (1) to disable the user interface (UI). False (0) to enable the UI. The value of the property is BOOL integer constant.

### setRefreshOverwriteAll() {#setrefreshoverwriteall}

Whether to overwrite user changes to shape data items in shapes linked to data when the data recordset is refreshed. The value of the property is BOOL integer constant.

### setReplaceLinks() {#setreplacelinks}

Defines how shape-data links are treated when shapes are copied or cut. 1 to replace existing links in the target shape. 0 to maintain existing links in the target shape. If this attribute is absent, Visio asks the user whether to replace links on copy or cut.

### setRowOrder() {#setroworder}

Whether to use the order of the rows in the data recordset as the primary key. True (1) if row IDs are determined by row order. False (0) if row IDs are determined by values in the primary key column(s) of the data recordset. The value of the property is BOOL integer constant.
