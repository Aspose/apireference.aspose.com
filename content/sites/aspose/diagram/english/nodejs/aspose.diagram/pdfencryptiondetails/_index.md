---
title: "PdfEncryptionDetails"
linktitle: "PdfEncryptionDetails"
second_title: "Aspose.Diagram for Node.js via Java API Reference"
description: "Contains details for a pdf encryption."
type: docs
weight: 1790
url: /nodejs/aspose.diagram/pdfencryptiondetails/
---

## PdfEncryptionDetails class

Contains details for a pdf encryption.

```js
new PdfEncryptionDetails()
```

Ctor.

## Methods

| Name | Description |
| --- | --- |
| [getEncryptionAlgorithm()](#getencryptionalgorithm) | Gets or sets the encryption mode. The value of the property is PdfEncryptionAlgorithm integer constant. |
| [getOwnerPassword()](#getownerpassword) | Gets or sets the Owner password. Opening the document with the correct owner password (assuming it is not the same as th |
| [getPermissions()](#getpermissions) | Gets or sets the permissions. The value of the property is PdfPermissions integer constant. |
| [getUserPassword()](#getuserpassword) | Gets or sets the User password. Opening the document with the correct user password (or opening a document that does not |
| [setEncryptionAlgorithm()](#setencryptionalgorithm) | Gets or sets the encryption mode. The value of the property is PdfEncryptionAlgorithm integer constant. |
| [setOwnerPassword()](#setownerpassword) | Gets or sets the Owner password. Opening the document with the correct owner password (assuming it is not the same as th |
| [setPermissions()](#setpermissions) | Gets or sets the permissions. The value of the property is PdfPermissions integer constant. |
| [setUserPassword()](#setuserpassword) | Gets or sets the User password. Opening the document with the correct user password (or opening a document that does not |

### getEncryptionAlgorithm() {#getencryptionalgorithm}

Gets or sets the encryption mode. The value of the property is PdfEncryptionAlgorithm integer constant.

### getOwnerPassword() {#getownerpassword}

Gets or sets the Owner password. Opening the document with the correct owner password (assuming it is not the same as the user password) allows full (owner) access to the document. This unlimited access includes the ability to change the document’s passwords and access permissions.

### getPermissions() {#getpermissions}

Gets or sets the permissions. The value of the property is PdfPermissions integer constant.

### getUserPassword() {#getuserpassword}

Gets or sets the User password. Opening the document with the correct user password (or opening a document that does not have a user password) allows additional operations to be performed according to the user access permissions specified in the document’s encryption dictionary.

### setEncryptionAlgorithm() {#setencryptionalgorithm}

Gets or sets the encryption mode. The value of the property is PdfEncryptionAlgorithm integer constant.

### setOwnerPassword() {#setownerpassword}

Gets or sets the Owner password. Opening the document with the correct owner password (assuming it is not the same as the user password) allows full (owner) access to the document. This unlimited access includes the ability to change the document’s passwords and access permissions.

### setPermissions() {#setpermissions}

Gets or sets the permissions. The value of the property is PdfPermissions integer constant.

### setUserPassword() {#setuserpassword}

Gets or sets the User password. Opening the document with the correct user password (or opening a document that does not have a user password) allows additional operations to be performed according to the user access permissions specified in the document’s encryption dictionary.
