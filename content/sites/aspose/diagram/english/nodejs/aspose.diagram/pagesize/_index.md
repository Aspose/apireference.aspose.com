---
title: "PageSize"
linktitle: "PageSize"
second_title: "Aspose.Diagram for Node.js via Java API Reference"
description: "Contains information about page size for the generated images."
type: docs
weight: 1740
url: /nodejs/aspose.diagram/pagesize/
---

## PageSize class

Contains information about page size for the generated images.

```js
new PageSize(paperSizeFormat)
```

Initializes a new instance of this class that can be used to set page size for the generated images.

## Methods

| Name | Description |
| --- | --- |
| [constructor_overload$1(width, height)](#constructor-overload1) | Initializes a new instance of this class that can be used to set page size for the generated images. |
| [getHeight()](#getheight) | Gets or sets the page height in points for the the generated images. The value must be > 0. If Height is set,Width must  |
| [getPaperSizeFormat()](#getpapersizeformat) | Gets or sets the paper size format for the generated images. Can be PaperSizeFormat. The value of the property is PaperS |
| [getWidth()](#getwidth) | Gets or sets the page width in points for the the generated images. The value must be > 0. If Width is set, Height must  |
| [setHeight()](#setheight) | Gets or sets the page height in points for the the generated images. The value must be > 0. If Height is set,Width must  |
| [setPaperSizeFormat()](#setpapersizeformat) | Gets or sets the paper size format for the generated images. Can be PaperSizeFormat. The value of the property is PaperS |
| [setWidth()](#setwidth) | Gets or sets the page width in points for the the generated images. The value must be > 0. If Width is set, Height must  |

### constructor_overload$1(width, height) {#constructor-overload1}

Initializes a new instance of this class that can be used to set page size for the generated images.

| Parameter | Type | Description |
| --- | --- | --- |
| width | Number | Page width in points for the the generated images. |
| height | Number | Page height in points for the the generated images. |

### getHeight() {#getheight}

Gets or sets the page height in points for the the generated images. The value must be > 0. If Height is set,Width must be set too.

### getPaperSizeFormat() {#getpapersizeformat}

Gets or sets the paper size format for the generated images. Can be PaperSizeFormat. The value of the property is PaperSizeFormat integer constant.

### getWidth() {#getwidth}

Gets or sets the page width in points for the the generated images. The value must be > 0. If Width is set, Height must be set too.

### setHeight() {#setheight}

Gets or sets the page height in points for the the generated images. The value must be > 0. If Height is set,Width must be set too.

### setPaperSizeFormat() {#setpapersizeformat}

Gets or sets the paper size format for the generated images. Can be PaperSizeFormat. The value of the property is PaperSizeFormat integer constant.

### setWidth() {#setwidth}

Gets or sets the page width in points for the the generated images. The value must be > 0. If Width is set, Height must be set too.
