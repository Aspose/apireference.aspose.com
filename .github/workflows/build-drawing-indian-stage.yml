name: Build Drawing Indian Stage

on:
  repository_dispatch:
    types: [ build-drawing-indian-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage.yml
    with:
      product_repo: "aspose-drawing/Aspose.Drawing-API-Reference"
      product_name: "drawing"
      disable_languages: '["en","ru","zh","ar","fr","de","el","id","it","ja","ko","nl","es","sv","tr"]'
      cf_path: "/drawing/*"
      theme_ref: "beta"
    secrets: inherit
