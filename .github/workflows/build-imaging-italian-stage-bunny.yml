name: Build Imaging Italian Stage (BunnyCDN)

on:
  repository_dispatch:
    types: [ build-imaging-italian-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage-bunny.yml
    with:
      product_repo: "aspose-imaging/Aspose.Imaging-API-References"
      product_name: "imaging"
      disable_languages: '["en","ru","zh","ar","fr","de","es","sv","tr"]'
      theme_ref: "beta"
    secrets: inherit
