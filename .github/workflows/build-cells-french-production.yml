name: Build Cells French Production

on:
  repository_dispatch:
    types: [ build-cells-french-production ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-production.yml
    with:
      product_repo: "aspose-cells/Aspose.Cells-API-References"
      product_name: "cells"
      disable_languages: '["en","ru","zh","ar","de","it","es","sv","tr"]'
      cf_path: "/cells/fr/*"
      theme_ref: "beta"
    secrets: inherit
