name: Build CAD English Stage

on:
  repository_dispatch:
    types: [ build-cad-english-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage.yml
    with:
      product_repo: "aspose-cad/Aspose.CAD-API-References"
      product_name: "cad"
      disable_languages: '["ru","zh"]'
      cf_path: "/cad/*"
      theme_ref: "beta"
    secrets: inherit
