name: Build Diagram Italian Production

on:
  repository_dispatch:
    types: [ build-diagram-italian-production ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-production.yml
    with:
      product_repo: "aspose-diagram/Aspose.Diagram-API-References"
      product_name: "diagram"
      disable_languages: '["en","ru","zh","ar","fr","de","el","hi","id","ja","ko","nl","es","sv","tr"]'
      cf_path: "/diagram/it/*"
      theme_ref: "beta"
    secrets: inherit
