name: Build OMR Japanese Stage

on:
  repository_dispatch:
    types: [ build-omr-japanese-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage.yml
    with:
      product_repo: "aspose-omr/Aspose.OMR-API-References"
      product_name: "omr"
      disable_languages: '["en","ru","zh","ar","fr","de","el","hi","id","it","ko","nl","es","sv","tr"]'
      cf_path: "/omr/*"
      theme_ref: "beta"
    secrets: inherit
