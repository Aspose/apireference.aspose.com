name: Build Imaging Spanish Stage

on:
  repository_dispatch:
    types: [ build-imaging-spanish-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage.yml
    with:
      product_repo: "aspose-imaging/Aspose.Imaging-API-References"
      product_name: "imaging"
      disable_languages: '["en","ru","zh","ar","fr","de","it","sv","tr"]'
      cf_path: "/imaging/*"
      theme_ref: "beta"
    secrets: inherit
