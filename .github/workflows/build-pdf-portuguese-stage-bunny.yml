name: Build Pdf Portuguese Stage (BunnyCDN)

on:
  repository_dispatch:
    types: [ build-pdf-portuguese-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage-bunny.yml
    with:
      product_repo: "aspose-pdf/Aspose.PDF-API-References"
      product_name: "pdf"
      disable_languages: '["en","ru","zh","ar","fr","de","id","it","ja","ko","es","sv","tr"]'
      theme_ref: "beta"
    secrets: inherit
