name: Build Omr Korean Production

on:
  repository_dispatch:
    types: [ build-omr-korean-production ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-production.yml
    with:
      product_repo: "aspose-omr/Aspose.OMR-API-References"
      product_name: "omr"
      disable_languages: '["en","ru","zh","ar","fr","de","el","hi","id","it","ja","nl","es","sv","tr"]'
      cf_path: "/omr/*"
      theme_ref: "beta"
    secrets: inherit
