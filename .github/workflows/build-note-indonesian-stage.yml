name: Build Note Indonesian Stage

on:
  repository_dispatch:
    types: [ build-note-indonesian-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage.yml
    with:
      product_repo: "aspose-note/Aspose.Note-API-References"
      product_name: "note"
      disable_languages: '["en","ru","zh","ar","fr","de","el","hi","it","ja","ko","nl","es","sv","tr"]'
      cf_path: "/note/*"
      theme_ref: "beta"
    secrets: inherit
