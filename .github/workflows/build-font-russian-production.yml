name: Build Font Russian Production

on:
  repository_dispatch:
    types: [ build-font-russian-production ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-production.yml
    with:
      product_repo: "aspose-font/Aspose.Font-API-References"
      product_name: "font"
      disable_languages: '["en","zh","ar","fr","de","it","es","sv","tr"]'
      cf_path: "/font/ru/*"
      theme_ref: "beta"
    secrets: inherit
