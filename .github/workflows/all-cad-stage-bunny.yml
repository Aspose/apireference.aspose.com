name: All CAD Staging Workflows (BunnyCDN)

on:
  workflow_dispatch:

jobs:
  trigger-all:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger build-cad-chinese-stage-bunny
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: build-cad-chinese-stage-bunny.yml
          token: ${{ secrets.REPO_TOKEN }}

      - name: Trigger build-cad-english-stage-bunny
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: build-cad-english-stage-bunny.yml
          token: ${{ secrets.REPO_TOKEN }}

      - name: Trigger build-cad-russian-stage-bunny
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: build-cad-russian-stage-bunny.yml
          token: ${{ secrets.REPO_TOKEN }}
