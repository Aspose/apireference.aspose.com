name: All Medical Staging Workflows (BunnyCDN)

on:
  workflow_dispatch:

jobs:
  trigger-all:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger build-medical-english-stage-bunny
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: build-medical-english-stage-bunny.yml
          token: ${{ secrets.REPO_TOKEN }}
