name: Build CAD Russian Stage (BunnyCDN)

on:
  repository_dispatch:
    types: [ build-cad-russian-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage-bunny.yml
    with:
      product_repo: "aspose-cad/Aspose.CAD-API-References"
      product_name: "cad"
      disable_languages: '["en","zh"]'
      theme_ref: "beta"
    secrets: inherit
