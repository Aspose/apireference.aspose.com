name: Build Barcode Chinese Stage

on:
  repository_dispatch:
    types: [ build-barcode-chinese-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage.yml
    with:
      product_repo: "aspose-barcode/Aspose.BarCode-API-References"
      product_name: "barcode"
      disable_languages: '["en","ru","ar","fr","de","el","hi","id","it","ja","ko","nl","es","sv","tr"]'
      cf_path: "/barcode/zh/*"
      theme_ref: "beta"
    secrets: inherit
