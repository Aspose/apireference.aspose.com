name: All LLM Staging Workflows (BunnyCDN)

on:
  workflow_dispatch:

jobs:
  trigger-all:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger build-llm-english-stage-bunny
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: build-llm-english-stage-bunny.yml
          token: ${{ secrets.REPO_TOKEN }}
