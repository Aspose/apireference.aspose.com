name: Build Diagram Chinese Stage

on:
  repository_dispatch:
    types: [ build-diagram-chinese-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage.yml
    with:
      product_repo: "aspose-diagram/Aspose.Diagram-API-References"
      product_name: "diagram"
      disable_languages: '["en","ru","ar","fr","de","el","hi","id","it","ja","ko","nl","es","sv","tr"]'
      cf_path: "/diagram/zh/*"
      theme_ref: "beta"
    secrets: inherit
