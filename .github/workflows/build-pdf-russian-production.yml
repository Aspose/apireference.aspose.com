name: Build Pdf Russian Production

on:
  repository_dispatch:
    types: [ build-pdf-russian-production ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-production.yml
    with:
      product_repo: "aspose-pdf/Aspose.PDF-API-References"
      product_name: "pdf"
      disable_languages: '["en","zh","ar","fr","de","id","it","ja","ko","pt","es","sv","tr"]'
      cf_path: "/pdf/*"
      theme_ref: "beta"
    secrets: inherit
