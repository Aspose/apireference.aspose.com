name: Build Cells Python NET Russian Stage

on:
  repository_dispatch:
    types: [ build-cells-python-net-russian-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage.yml
    with:
      product_repo: "aspose-cells/Aspose.Cells-Python-NET-API-References"
      product_name: "cells-python-net"
      disable_languages: '["en","zh","ar","fr","de","it","es","sv","tr"]'
      cf_path: "/cells/python-net/ru/*"
      theme_ref: "beta"
    secrets: inherit
