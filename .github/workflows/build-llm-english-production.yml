name: Build LLM English Production

on:
  repository_dispatch:
    types: [ build-llm-english-production ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-production.yml
    with:
      product_repo: "aspose-llm/Aspose.LLM-References"
      product_name: "llm"
      disable_languages: '[]'
      cf_path: "/llm/*"
      theme_ref: "beta"
    secrets: inherit
