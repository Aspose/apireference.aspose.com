name: Build Font Chinese Production

on:
  repository_dispatch:
    types: [ build-font-chinese-production ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-production.yml
    with:
      product_repo: "aspose-font/Aspose.Font-API-References"
      product_name: "font"
      disable_languages: '["en","ru","ar","fr","de","it","es","sv","tr"]'
      cf_path: "/font/zh/*"
      theme_ref: "beta"
    secrets: inherit
