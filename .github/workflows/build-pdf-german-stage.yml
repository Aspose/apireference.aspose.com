name: Build PDF German Stage

on:
  repository_dispatch:
    types: [ build-pdf-german-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage.yml
    with:
      product_repo: "aspose-pdf/Aspose.PDF-API-References"
      product_name: "pdf"
      disable_languages: '["en","ru","zh","ar","fr","id","it","ja","ko","pt","es","sv","tr"]'
      cf_path: "/pdf/*"
      theme_ref: "beta"
    secrets: inherit
