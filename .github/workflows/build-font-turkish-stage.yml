name: Build Font Turkish Stage

on:
  repository_dispatch:
    types: [ build-font-turkish-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage.yml
    with:
      product_repo: "aspose-font/Aspose.Font-API-References"
      product_name: "font"
      disable_languages: '["en","ru","zh","ar","fr","de","it","es","sv"]'
      cf_path: "/font/tr/*"
    secrets: inherit
