name: Build Diagram Indian Stage

on:
  repository_dispatch:
    types: [ build-diagram-indian-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage.yml
    with:
      product_repo: "aspose-diagram/Aspose.Diagram-API-References"
      product_name: "diagram"
      disable_languages: '["en","ru","zh","ar","fr","de","el","id","it","ja","ko","nl","es","sv","tr"]'
      cf_path: "/diagram/hi/*"
      theme_ref: "beta"
    secrets: inherit
