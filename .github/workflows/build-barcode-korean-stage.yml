name: Build Barcode Korean Stage

on:
  repository_dispatch:
    types: [ build-barcode-korean-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage.yml
    with:
      product_repo: "aspose-barcode/Aspose.BarCode-API-References"
      product_name: "barcode"
      disable_languages: '["en","ru","zh","ar","fr","de","el","hi","id","it","ja","nl","es","sv","tr"]'
      cf_path: "/barcode/ko/*"
      theme_ref: "beta"
    secrets: inherit
