name: Build Page Netherlands Stage (BunnyCDN)

on:
  repository_dispatch:
    types: [ build-page-netherlands-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage-bunny.yml
    with:
      product_repo: "aspose-page/Aspose.Page-API-References"
      product_name: "page"
      disable_languages: '["en","ru","zh","ar","fr","de","el","hi","id","it","ja","ko","es","sv","tr"]'
      theme_ref: "beta"
    secrets: inherit
