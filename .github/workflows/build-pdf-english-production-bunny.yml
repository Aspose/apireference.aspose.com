name: Build Pdf English Production (BunnyCDN)

on:
  repository_dispatch:
    types: [ build-pdf-english-production ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-production-bunny.yml
    with:
      product_repo: "aspose-pdf/Aspose.PDF-API-References"
      product_name: "pdf"
      disable_languages: '["ru","zh","ar","fr","de","id","it","ja","ko","pt","es","sv","tr"]'
      theme_ref: "beta"
    secrets: inherit
