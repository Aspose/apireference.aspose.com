name: Build Tasks Indonesian Stage (BunnyCDN)

on:
  repository_dispatch:
    types: [ build-tasks-indonesian-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage-bunny.yml
    with:
      product_repo: "aspose-tasks/Aspose.Tasks-API-References"
      product_name: "tasks"
      disable_languages: '["en","ru","zh","ar","fr","de","el","hi","it","ja","ko","nl","es","sv","tr"]'
      theme_ref: "beta"
    secrets: inherit
