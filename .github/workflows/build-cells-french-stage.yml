name: Build Cells French Stage

on:
  repository_dispatch:
    types: [ build-cells-french-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage.yml
    with:
      product_repo: "aspose-cells/Aspose.Cells-API-References"
      product_name: "cells"
      disable_languages: '["en","ru","zh","ar","de","it","es","sv","tr"]'
      cf_path: "/cells/fr/*"
      theme_ref: "beta"
    secrets: inherit
