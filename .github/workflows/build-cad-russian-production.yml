name: Build CAD Russian Production

on:
  repository_dispatch:
    types: [ build-cad-russian-production ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-production.yml
    with:
      product_repo: "aspose-cad/Aspose.CAD-API-References"
      product_name: "cad"
      disable_languages: '["en","zh"]'
      cf_path: "/cad/*"
      theme_ref: "beta"
    secrets: inherit
