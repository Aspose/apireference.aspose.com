name: Build Font Swedish Stage

on:
  repository_dispatch:
    types: [ build-font-swedish-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage.yml
    with:
      product_repo: "aspose-font/Aspose.Font-API-References"
      product_name: "font"
      disable_languages: '["en","ru","zh","ar","fr","de","it","es","tr"]'
      cf_path: "/font/sv/*"
      theme_ref: "beta"
    secrets: inherit
