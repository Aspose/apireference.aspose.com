name: Build Barcode Netherlands Stage

on:
  repository_dispatch:
    types: [ build-barcode-netherlands-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage.yml
    with:
      product_repo: "aspose-barcode/Aspose.BarCode-API-References"
      product_name: "barcode"
      disable_languages: '["en","ru","zh","ar","fr","de","el","hi","id","it","ja","ko","es","sv","tr"]'
      cf_path: "/barcode/nl/*"
      theme_ref: "beta"
    secrets: inherit
