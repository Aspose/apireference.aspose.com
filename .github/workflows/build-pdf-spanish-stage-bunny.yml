name: Build Pdf Spanish Stage (BunnyCDN)

on:
  repository_dispatch:
    types: [ build-pdf-spanish-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage-bunny.yml
    with:
      product_repo: "aspose-pdf/Aspose.PDF-API-References"
      product_name: "pdf"
      disable_languages: '["en","ru","zh","ar","fr","de","id","it","ja","ko","pt","sv","tr"]'
      theme_ref: "beta"
    secrets: inherit
