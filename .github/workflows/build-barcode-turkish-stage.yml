name: Build Barcode Turkish Stage

on:
  repository_dispatch:
    types: [ build-barcode-turkish-stage ]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build-stage.yml
    with:
      product_repo: "aspose-barcode/Aspose.BarCode-API-References"
      product_name: "barcode"
      disable_languages: '["en","ru","zh","ar","fr","de","el","hi","id","it","ja","ko","nl","es","sv"]'
      cf_path: "/barcode/tr/*"
      theme_ref: "beta"
    secrets: inherit
